Slots
-----
1) main event
Next to the one time event, there is a button called slot
Opens a new slots wizard popup (calendar view - dragable element, and you can add slots - you can click on a cell and drag arround,
which attaches and detaches automatically aligned to the cell / 4 = by 15 minutes - so how it works the calendar can switch between da and week view)
The main event editor, just show the slots in read only form, maybe scrolabble (undecided)
Naturally the frequency affects the slot what you can add - monthly - you can pick some days, but on daily not.
2) request to join on event explore (you can fully book an event - a flag on main event, but in this case the request to join is a "book")
when you request join slots vizard in read only form popups up and you can choose a slot
(the request join menu itself should show red counter or different way that you need to choose from slots)
3) slots has separate and coloured chat channels
4) for creator for each slot a new event item will appear on activity event - it will be automatically generated for future dates and has dedicated custom colouring

Asset explore
-------------
You can borrow items from a foreigner (assets (accomodation/car/suplies) with public or friends visibility)
Assets can be filtered by capacity, availability, topic (very similar screen then the event explore)
Assets can have price tag (we haven't introduced purse for public/frineds event also)

Bugs
----
1) pull up feature if you ended up bottom of the scrolling area
2) extract the templates and functionalities from activities-popup.component.ts and put under a templates folder next to it (we have three templates here - rate/event/chat)
3) optional events can be parallel (maybe a switch on the form "parallel allowed"), but needs to cut the mandatory in the same way when you add

Speed
-----
4) curl rating/scrolling animation speed up in most of the places
5) demo creation has got some noticeable slowness on mobile

6) instagram account should be renamed from myscoutee1 to myscoutee, after the other account deleted (deletion period ended apr 3)

7) put a contacts button to show assign members popup without basket
8) on profile editor the school/workspace item can be loaded from a cv (pdf/docx parse)